# Dependencies

Complete reference of all dependencies used in this Next.js portfolio project.

## Production Dependencies

### Core Framework

| Package     | Version | Purpose                                   |
| ----------- | ------- | ----------------------------------------- |
| `next`      | 15.2.4  | React framework with SSR/SSG capabilities |
| `react`     | ^19     | Core React library                        |
| `react-dom` | ^19     | React DOM renderer                        |

**Why Next.js 15?**

- Latest stable version with App Router
- React Server Components support
- Improved performance and bundle size
- Better developer experience

### UI Component Library (Radix UI)

The project uses **Shadcn/ui**, which is built on top of **Radix UI** primitives. These provide accessible, unstyled components that are then styled with Tailwind CSS.

| Package                           | Purpose                       |
| --------------------------------- | ----------------------------- |
| `@radix-ui/react-accordion`       | Collapsible content sections  |
| `@radix-ui/react-alert-dialog`    | Modal alert dialogs           |
| `@radix-ui/react-aspect-ratio`    | Aspect ratio containers       |
| `@radix-ui/react-avatar`          | User avatar display           |
| `@radix-ui/react-checkbox`        | Checkbox inputs               |
| `@radix-ui/react-collapsible`     | Collapsible content           |
| `@radix-ui/react-context-menu`    | Right-click context menus     |
| `@radix-ui/react-dialog`          | Dialog/modal components       |
| `@radix-ui/react-dropdown-menu`   | Dropdown menus                |
| `@radix-ui/react-hover-card`      | Hover card overlays           |
| `@radix-ui/react-label`           | Form labels                   |
| `@radix-ui/react-menubar`         | Menu bar navigation           |
| `@radix-ui/react-navigation-menu` | Navigation menus              |
| `@radix-ui/react-popover`         | Popover overlays              |
| `@radix-ui/react-progress`        | Progress bars                 |
| `@radix-ui/react-radio-group`     | Radio button groups           |
| `@radix-ui/react-scroll-area`     | Scrollable areas              |
| `@radix-ui/react-select`          | Select dropdowns              |
| `@radix-ui/react-separator`       | Visual separators             |
| `@radix-ui/react-slider`          | Range sliders                 |
| `@radix-ui/react-slot`            | Component composition utility |
| `@radix-ui/react-switch`          | Toggle switches               |
| `@radix-ui/react-tabs`            | Tab navigation                |
| `@radix-ui/react-toast`           | Toast notifications           |
| `@radix-ui/react-toggle`          | Toggle buttons                |
| `@radix-ui/react-toggle-group`    | Toggle button groups          |
| `@radix-ui/react-tooltip`         | Tooltips                      |

**Key Benefits:**

- ✅ Fully accessible (ARIA, keyboard navigation)
- ✅ Unstyled primitives (full design control)
- ✅ Composable and flexible
- ✅ Well-documented and maintained

### Styling & Theming

| Package                    | Version  | Purpose                              |
| -------------------------- | -------- | ------------------------------------ |
| `tailwindcss`              | ^4.1.9   | Utility-first CSS framework          |
| `autoprefixer`             | ^10.4.20 | CSS vendor prefixing                 |
| `@tailwindcss/postcss`     | ^4.1.9   | Tailwind PostCSS plugin              |
| `postcss`                  | ^8.5     | CSS transformation tool              |
| `tailwindcss-animate`      | ^1.0.7   | Animation utilities for Tailwind     |
| `tw-animate-css`           | 1.3.3    | Additional CSS animations            |
| `next-themes`              | ^0.4.6   | Theme management (dark/light mode)   |
| `class-variance-authority` | ^0.7.1   | Variant-based component styling      |
| `clsx`                     | ^2.1.1   | Conditional className utility        |
| `tailwind-merge`           | ^2.5.5   | Intelligently merge Tailwind classes |

**Styling Stack:**

```tsx
// Typical usage
import { cn } from "@/lib/utils";

<div
  className={cn(
    "base-classes",
    variant === "primary" && "primary-classes",
    className
  )}
/>;
```

### Form Management

| Package               | Version | Purpose                            |
| --------------------- | ------- | ---------------------------------- |
| `react-hook-form`     | ^7.60.0 | Performant form state management   |
| `@hookform/resolvers` | ^3.10.0 | Schema validation resolvers        |
| `zod`                 | 3.25.67 | TypeScript-first schema validation |

**Example Usage:**

```tsx
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import * as z from "zod";

const schema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
});

const form = useForm({
  resolver: zodResolver(schema),
});
```

### UI Enhancement Libraries

| Package                  | Version  | Purpose                                 |
| ------------------------ | -------- | --------------------------------------- |
| `lucide-react`           | ^0.454.0 | Beautiful, consistent icon library      |
| `sonner`                 | ^1.7.4   | Elegant toast notifications             |
| `cmdk`                   | 1.0.4    | Command menu (⌘K style interface)       |
| `embla-carousel-react`   | 8.5.1    | Smooth carousel/slider component        |
| `react-day-picker`       | 9.8.0    | Date picker component                   |
| `input-otp`              | 1.4.1    | OTP (One-Time Password) input           |
| `recharts`               | 2.15.4   | Charting library for data visualization |
| `react-resizable-panels` | ^2.1.7   | Resizable panel layouts                 |

### Typography & Fonts

| Package     | Version    | Purpose                           |
| ----------- | ---------- | --------------------------------- |
| `geist`     | ^1.3.1     | Geist font family (Vercel's font) |
| `next/font` | (built-in) | Automatic font optimization       |

**Font Usage:**

```tsx
import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});
```

### Utility Libraries

| Package    | Version | Purpose                       |
| ---------- | ------- | ----------------------------- |
| `date-fns` | 4.1.0   | Modern date utility functions |

## Development Dependencies

| Package            | Version | Purpose                     |
| ------------------ | ------- | --------------------------- |
| `typescript`       | ^5      | TypeScript language support |
| `@types/node`      | ^22     | Node.js type definitions    |
| `@types/react`     | ^19     | React type definitions      |
| `@types/react-dom` | ^19     | React DOM type definitions  |

## NPM Scripts

### Available Commands

```json
{
  "dev": "next dev", // Start development server
  "build": "next build", // Build for production
  "start": "next start", // Start production server
  "lint": "next lint" // Run ESLint
}
```

### Command Details

#### Development Server

```bash
npm run dev
```

- Starts Next.js dev server on `http://localhost:3000`
- Hot Module Replacement (HMR) enabled
- Fast Refresh for instant feedback
- Shows compilation errors in browser

#### Production Build

```bash
npm run build
```

- Creates optimized production build
- Generates static HTML where possible
- Optimizes images and fonts
- Minifies JavaScript and CSS
- Shows bundle analysis

#### Production Server

```bash
npm run start
```

- Starts production server (requires `npm run build` first)
- Serves optimized build
- No hot reload (production mode)

#### Linting

```bash
npm run lint
```

- Runs ESLint on the codebase
- Checks for code quality issues
- Enforces coding standards

## Dependency Management

### Installation

```bash
# Using npm
npm install

# Using pnpm (recommended for monorepos)
pnpm install

# Using yarn
yarn install
```

### Adding New Dependencies

```bash
# Production dependency
npm install <package-name>

# Development dependency
npm install -D <package-name>

# Specific version
npm install <package-name>@<version>
```

### Shadcn/ui Components

To add new UI components:

```bash
npx shadcn@latest add <component-name>

# Examples:
npx shadcn@latest add button
npx shadcn@latest add dialog
npx shadcn@latest add form
```

This downloads the component source to `components/ui/` where you can customize it.

## Version Strategy

### Semantic Versioning

- `^15.2.4` - Allows updates to `15.x.x` (minor and patch)
- `~15.2.4` - Allows updates to `15.2.x` (patch only)
- `15.2.4` - Exact version (locked)

### Recommended Updates

**Critical Updates:**

- Security patches (immediate)
- Next.js minor versions (review changelog)
- React major versions (thorough testing)

**Regular Updates:**

- UI library patches (monthly)
- Development dependencies (quarterly)

## Bundle Size Optimization

### Current Bundle Analysis

Key strategies used:

- **Tree Shaking**: Remove unused code
- **Code Splitting**: Automatic per route
- **Dynamic Imports**: Load on demand
- **Image Optimization**: Next.js Image component
- **Font Subsetting**: Only load used characters

### Monitoring Bundle Size

```bash
# Install bundle analyzer
npm install -D @next/bundle-analyzer

# Analyze
ANALYZE=true npm run build
```

## Dependency Highlights

### Most Important Dependencies

1. **next** (15.2.4) - The core framework
2. **react** (^19) - UI library
3. **tailwindcss** (^4.1.9) - Styling solution
4. **@radix-ui/** - Accessible UI primitives
5. **typescript** (^5) - Type safety

### Unique/Notable Choices

- **OKLCH Colors**: Modern, perceptually uniform color space
- **Radix UI**: Headless UI primitives for full design control
- **React 19**: Bleeding edge React features
- **Tailwind CSS 4**: Latest with new features
- **next-themes**: Seamless theme switching

---

**Previous:** [← Project Structure](./01-project-structure.md) | **Next:** [Next.js & SSR →](./03-nextjs-and-ssr.md)
